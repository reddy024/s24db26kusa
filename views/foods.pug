extends layout

block content
  h1 Searching for foods
  .container-fluid
    .row
      -
      for food in foods 
        .col-sm-2
          h3= `Name: ${food.name}`
          p= `Category: ${food.category}`
          p= `Price: ${food.price}`
